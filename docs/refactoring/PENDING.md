# Pending — everything documented and not yet done

> The derived index over the plans in `implementation_plan/`. Each item names its plan,
> its gate, and its state. Read this first when onboarding (the `/onboard` flow).

| # | Item | Plan | Gate | State |
|:--|:---|:---|:---|:---|
| 1 | FP0.9 — port the lifecycle skills | `implementation_plan/FLUTTER_PORT_PLAN_2026-08-25.md` | skills fire on their triggers | pending (rest of FP0 gate met 2026-08-25) |
| 2 | FP1.5/FP1.9 — component slice + domain `*Rules` ports | same plan | first components (health/movement) + rules with tests | first slice done 2026-08-26 (Direction/Movement/Health + 4 Rules); more vitals/AI components arrive with their systems |
| 3 | FP2.3 — sprites steps (02/03) | same plan | atlas cropped into sprite PNGs | done 2026-08-26 (22 crops + 14 placeholders; Pillow; `res://data/…` mapped to `assets/generated/<game>/…`). FP2.4/FP2.5 done 2026-08-26. Note: the Godot repo restructured (`tessera.py`, pipeline under `tessera/scripts/`) — cross-repo references in our docs may need a sweep |
| 4 | FP3 — World & rendering (Flame) | same plan | walkable chunked world at 60fps with budget overlay | **gate met 2026-08-26** — hand-run on the macOS app held **120fps** (the display's own ceiling) walking one direction continuously, which is the load pattern that stresses streaming hardest: load at the leading edge and unload at the trailing one, every chunk border, plus each new chunk's spawn burst since 0.19.0. A sustained 8.3ms frame against a 1200µs stream budget is ~2× the headroom the 60fps gate asked for. FP3.1–3.3 done 2026-08-26; FP3.4 in five commits (0.8.0–0.12.0); FP3.5 grid collision (0.13.0); FP3.6 overlay (0.14.0) |
| 5 | FP4 — First gameplay loop | same plan | harvest → craft → place playable | FP4.1 done 2026-08-26 in four commits (0.15.0 loot tables + biome population parse, 0.16.0 DropRules, 0.18.0 physical pickups, 0.19.0 procedural scatter + prop occupancy); FP4.2a inventory domain done (0.17.0). **FP4.2b done 2026-08-27** in seven commits — see #7. **FP4.3a done 2026-08-29** — the swing, and with it the first half of the gate: HARVEST is playable (see #8). **FP4.3b done 2026-08-30** — the place verb works end to end, and is not yet REACHABLE (see #9). **Remaining: FP4.4** (farm transform), **FP4.5** (crafting — the gate; it is also what makes FP4.3b reachable, and the content for it is not imported yet). Ground destruction deferred to FP7 by decision |
| 6 | FP5–FP7 | same plan | per-phase gates | pending. FP5.1's `UIStateMachine` + back-press arbiter arrived early with FP4.2b (0.22.0) — what remains there is the HUD itself and the notification queue |
| 7 | FP4.2b — hotbar + inventory surface | same plan | the bag is openable, draggable and readable in both languages | **done 2026-08-27.** 0.20.0 interface strings enter the pipeline (`data/ui/`, step 05's second source) · 0.21.0 slot verbs · 0.22.0 `UIStateMachine` + rule-25 arbiter (pulled forward from FP5.1) · 0.23.0 the player becomes an authored actor with a 30-slot bag (was a boar with none; the pipeline learned multiple object roots) · 0.24.0 the hotbar · 0.24.2 the authored `pickup_delay`, without which nothing can be put down · 0.25.0 the panel (drag swap/merge, Shift-drag half, drop-to-world, live `BackdropFilter`). **Two things were deliberately NOT in it, and one of them is now paid.** The **Sort button landed 2026-08-31** in three commits (0.41.0 `InventorySortRules`, the seven-step ladder with only three live subjects and every number held · 0.42.0 `InventoryComponent.sortItems` + `compareStacks` + the fold and the cut, announcing only the slots that moved · 0.43.0 the button, `ui.inventory.sort` in three locales, and `backpack.md` in both languages, whose "Not yet" section had said in writing that the button was being withheld). Its two blockers had both arrived: `tier` at 0.27.0 and `ItemBuildableData` at 0.35.0. **PORT DELTA carried forward:** the fourth sort axis — the item's localized display name — has no subject, because no data class reads the `display_name_key` the pipeline already emits; the order falls to the spec's own next axis, the id, and the name arrives with the first surface that shows one (a tooltip, FP5). Still open: mouse-wheel hotbar cycling, which needs pointer state in `InputHelper` (rule 11 forbids reading it anywhere else). The spec's carry-state split is not planned — Shift-drag replaced it by decision |
| 8 | FP4.3a — verbs & permissions | `implementation_plan/FLUTTER_PORT_PLAN_2026-08-25.md` | a swing is refused or allowed for one reason, in one place | **DONE 2026-08-29**, in nine commits (0.26.0–0.34.0). The gate is one place and one order, and the loop it feeds is playable: point at a tree, click or tap, and it falls. 0.26.0 `ActorOccupancyHelper` + `ActorTracker` · 0.27.0 `WorldObjectDeathRules` + the three authored fields nothing read (`tier`, `allowedTools`, nullable `toolType`) · 0.28.0 `HeldItemComponent` — the selected slot IS the hand, derived not stored; an empty slot is BARE HANDS for a player · 0.29.0 `WorldObjectPermissionHelper` + `WorldObjectToolHelper` — the ordered gate, both order traps under test, target as a sealed `DamageTarget` (ground is a TILE here, FP3.4) · 0.30.0 props gained health and drops; a death releases its tiles BEFORE the loot rolls, and the spawn system forgets a corpse (a double free that `freePropTiles` asserts on) · 0.31.0 `IActor.usePrimaryActionOn` + `attackDamage` · 0.32.0 the unified cursor in `InputHelper` (mouse + touch) · 0.33.0 `AimSnapshot` — measured once, never re-derived · 0.34.0 `ActorPlayer`: strict single-tile targeting, edge-to-edge reach, `1/base_action_speed` pacing, rule-30 blocker. Manual page `gathering.md` in both languages. **What FP4.3a deliberately did NOT do, each with its subject named:** the swing has no MOTION on screen (`HeldItemComponent`'s tween half, with the impact delay `AimSnapshot` exists to survive); actors cannot be hit (no corpse, loot or respawn — so `usePrimaryActionOn` takes a `Prop`, a missing door rather than a branch that answers no — 0.38.0 renamed it `usePrimaryAction(AimSnapshot)`, and actors still cannot be hit); the GAMEPAD's virtual cursor is unported, which is rule 12 broken in writing (it also blocks FP4.2b's mouse-wheel hotbar); and the action costs — energy, mana, combo points — wait with the rest of `HeldItemComponent`. |
| 9 | FP4.3b — placement | `implementation_plan/FLUTTER_PORT_PLAN_2026-08-25.md` | a blueprint in hand puts the thing into the world, or says why not | **DONE 2026-08-30**, in six commits (0.35.0–0.40.0). With a blueprint in hand a press puts the thing into the world, and a see-through copy shows where it would land — green where it may, red where it may not. 0.35.0 `ItemBuildableData` (a `blueprint_id`, the blueprint resolved through a registry) + type routing in `ItemRegistry`; five documents imported from the spec, and the loot-id sweep learned to cover GROUND · 0.36.0 `WorldPlacementHelper.placePropRefusal` + `enum PlacementRefusal` — a refusal that is NAMED — and `GridManager.canPlacePropAt`; occupancy is the FIRST gate, above every early return, and two tests fix the order by refusing a tile two gates refuse and asserting which one answers · 0.37.0 the ground half (water and cliff are replaced) + `Events.groundTileChanged` → `GroundChunkRenderer` re-bakes the chunk · 0.38.0 REFACTOR: `ItemHand` + `ItemHandTool`, `usePrimaryAction(AimSnapshot)`, `HeldItemComponent.hand` picking the hand, and `enum ActionOutcome {none, spent, landed}` — vocabulary this port invented, not the spec's · 0.39.0 `ItemHandBuildable`, the verb itself in four steps (tiles → gate → spend → place), `isAreaWithinRange` extracted so building and striking share ONE geometry, `ProceduralSpawnSystem.adoptPlacedProp` so a built prop leaves with its chunk · 0.40.0 `BuildGhostRenderer` + `previewAt` — the preview and the press read the SAME resolution (`BuildPreview`), so the ghost cannot lie about the click. **A seventh slice was planned, investigated and REFUSED:** `canBuildOnTarget` in `WorldObjectPermissionHelper`. Its subject is no longer the missing data class — `ItemBuildableData` exists since 0.35.0 — but the missing SECOND CONSUMER: `canDamageTarget`'s only caller here is `ItemHandTool.primaryAction`, and `_rebuildHand` routes every blueprint to the build hand before the tool arm, so the source can never hold one when that gate is asked. The spec's branch is reachable only because `interaction_indicator.gd` asks it too, and that indicator is FP5.1. The helper's own note now says this (0.44.0). **Two debts written down rather than forgotten:** (1) **nothing in the game can obtain a blueprint** — no prop drops an `item_buildable_*`, the three seed blueprints belong to `stage_drop_configs` (FP4.4), grass comes from destroying ground (FP7), and the smelter comes from breaking a smelter nobody placed; the player authors no starting inventory. Placing becomes reachable at **FP4.5**, whose crafting menu authors the smelter at `crafted_at: NONE`. That is why 0.39.0 and 0.40.0 are `### 🧹 Internal` and not `### ✨ New`, and why the manual page `building.md` is DEFERRED to the FP4.5 commit that makes the verb reachable — documenting a deed the player cannot perform would be a lie. (2) **stacking ground on ground is refused WHOLE** (`cannotStack`), not half-ported: the spec's first gate alone would leave a branch that says yes to the terrain and then has nowhere to go, because max height, the world edge and the slab margin are all FP7. **Also true and deliberate:** a placed tile is DATA, not a node (terrain is nodeless, FP3.4); and a built prop or bridge DISAPPEARS when its chunk recycles, the same bargain a harvested prop already makes — surviving an unload is FP6. Ledger `L-006` and `L-007` were opened by this work. |
