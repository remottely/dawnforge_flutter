# Pending — everything documented and not yet done

> The derived index over the plans in `implementation_plan/`. Each item names its plan,
> its gate, and its state. Read this first when onboarding (the `/onboard` flow).

| # | Item | Plan | Gate | State |
|:--|:---|:---|:---|:---|
| 1 | FP0.9 — port the lifecycle skills | `implementation_plan/FLUTTER_PORT_PLAN_2026-08-25.md` | skills fire on their triggers | pending (rest of FP0 gate met 2026-08-25). **2026-09-10:** the harness debt the rules already assume — `reset_local_save.py` (L-004), the pack-drift `--check` (L-006), the merge-commit path (L-008), the cross-repo path sweep (L-003), the `dynamic` tripwire (L-009), the manual's section map + mirror check (L-010), changelog-order and translation-key guards, `docs/ARCHITECTURE.md` — is written down as **FP0.10–FP0.18**; see #12 | **Sliced 2026-09-10 (0.60.0) into (a)–(f)**, and the one-line version was short: the spec carries 21 skills (18 its own, 3 installed generics) and this repo has none — `.claude/skills/` has never existed, so FP0.4's index points at an empty subject. The bullet named 10 and omitted five that apply: `decision`, `pipeline`, `rule-audit`, `metrics`, `grill-me`. `ship` now waits on `AUTOMATION_DEBT_2026-09-10.md` AD3.1/AD3.2 rather than teaching a recipe about to be replaced by a script.
| 2 | FP1.5/FP1.9 — component slice + domain `*Rules` ports | same plan | first components (health/movement) + rules with tests | first slice done 2026-08-26 (Direction/Movement/Health + 4 Rules); more vitals/AI components arrive with their systems |
| 3 | FP2.3 — sprites steps (02/03) | same plan | atlas cropped into sprite PNGs | done 2026-08-26 (22 crops + 14 placeholders; Pillow; `res://data/…` mapped to `assets/generated/<game>/…`). FP2.4/FP2.5 done 2026-08-26. Note: the Godot repo restructured (`tessera.py`, pipeline under `tessera/scripts/`) — cross-repo references in our docs may need a sweep |
| 4 | FP3 — World & rendering (Flame) | same plan | walkable chunked world at 60fps with budget overlay | **gate met 2026-08-26** — hand-run on the macOS app held **120fps** (the display's own ceiling) walking one direction continuously, which is the load pattern that stresses streaming hardest: load at the leading edge and unload at the trailing one, every chunk border, plus each new chunk's spawn burst since 0.19.0. A sustained 8.3ms frame against a 1200µs stream budget is ~2× the headroom the 60fps gate asked for. FP3.1–3.3 done 2026-08-26; FP3.4 in five commits (0.8.0–0.12.0); FP3.5 grid collision (0.13.0); FP3.6 overlay (0.14.0) | **2026-09-10 (0.64.0):** the study's risk register got its first status pass, and risk 1 leaves a step nobody owns — that 120fps was measured on 2026-08-26, before prop scatter, actor tracking, the aim snapshot and the production queue joined the fixed step, and nothing re-runs it. The rule proposed there: **a gate proven by hand is re-proven by hand at the next gate**, the new number written into the progress table beside the old. FP5's gate is the next one due.
| 5 | FP4 — First gameplay loop | same plan | harvest → craft → place playable | FP4.1 done 2026-08-26 in four commits (0.15.0 loot tables + biome population parse, 0.16.0 DropRules, 0.18.0 physical pickups, 0.19.0 procedural scatter + prop occupancy); FP4.2a inventory domain done (0.17.0). **FP4.2b done 2026-08-27** in seven commits — see #7. **FP4.3a done 2026-08-29** — the swing, and with it the first half of the gate: HARVEST is playable (see #8). **FP4.3b done 2026-08-30** — the place verb works end to end, and is not yet REACHABLE (see #9). **Remaining: FP4.4** (farm transform), **FP4.5** (crafting — the gate; in progress since 2026-08-31, content imported and recipe data read at 0.45.0). **The gate needs one thing the port cannot supply on its own:** the authored content has a cold-start deadlock (ore and coal need a PICKAXE; the pickaxe needs a WORKSHOP and a SMELTER; the smelter needs ore and coal), which holds on the delivery track too and is opened there only by a commented-out debug dict. Decision 2026-08-31: an **authored starting inventory** on the player document, which also obliges the hand-craft surface `crafted_at: NONE` has never had. The plan's FP4.5 bullet carries the evidence and corrects the claim 0.44.0 got wrong. Ground destruction deferred to FP7 by decision. **2026-09-10:** FP4.5(d) `WorkstationComponent` + `ProductionRules` landed at **0.48.0**; the remaining (e) interact verb · (f) two surfaces · (g) bootstrap · (h) manual are re-sliced in the plan and the bootstrap decision is **corrected** (see #10). **FP4.4 moves AFTER FP4.5** — every farm tool is a workshop recipe (see #11) |
| 6 | FP5–FP7 | same plan | per-phase gates | pending. FP5.1's `UIStateMachine` + back-press arbiter arrived early with FP4.2b (0.22.0) — what remains there is the HUD itself and the notification queue. **Sliced 2026-09-10:** FP5.1(a)–(h), FP5.2(a)–(g), FP5.3; FP6.1–FP6.4 (`SaveManager` sections owned by their systems, the per-chunk DIFF-from-the-roll envelope, the catalogue for the world list); FP7.1–FP7.15 as a dependency map with a sub-gate each. The plan now carries a **Decision register** (`D-1`…`D-8`) — the forks that need the developer, each with a recommendation, none decided by this pass | **2026-09-10 (0.65.0):** the register gained `D-9` — the intent layer study decision D4 names as one of three multiplayer door-keepers has no subject in the code (`L-018`), and its cost grows by one call site per verb, so the fork is put before FP4.5(e) and FP4.4(g) rather than after FP7.7.
| 7 | FP4.2b — hotbar + inventory surface | same plan | the bag is openable, draggable and readable in both languages | **done 2026-08-27.** 0.20.0 interface strings enter the pipeline (`data/ui/`, step 05's second source) · 0.21.0 slot verbs · 0.22.0 `UIStateMachine` + rule-25 arbiter (pulled forward from FP5.1) · 0.23.0 the player becomes an authored actor with a 30-slot bag (was a boar with none; the pipeline learned multiple object roots) · 0.24.0 the hotbar · 0.24.2 the authored `pickup_delay`, without which nothing can be put down · 0.25.0 the panel (drag swap/merge, Shift-drag half, drop-to-world, live `BackdropFilter`). **Two things were deliberately NOT in it, and one of them is now paid.** The **Sort button landed 2026-08-31** in three commits (0.41.0 `InventorySortRules`, the seven-step ladder with only three live subjects and every number held · 0.42.0 `InventoryComponent.sortItems` + `compareStacks` + the fold and the cut, announcing only the slots that moved · 0.43.0 the button, `ui.inventory.sort` in three locales, and `backpack.md` in both languages, whose "Not yet" section had said in writing that the button was being withheld). Its two blockers had both arrived: `tier` at 0.27.0 and `ItemBuildableData` at 0.35.0. **PORT DELTA carried forward:** the fourth sort axis — the item's localized display name — has no subject, because no data class reads the `display_name_key` the pipeline already emits; the order falls to the spec's own next axis, the id, and the name arrives with the first surface that shows one (a tooltip, FP5). Still open: mouse-wheel hotbar cycling, which needs pointer state in `InputHelper` (rule 11 forbids reading it anywhere else). The spec's carry-state split is not planned — Shift-drag replaced it by decision |
| 8 | FP4.3a — verbs & permissions | `implementation_plan/FLUTTER_PORT_PLAN_2026-08-25.md` | a swing is refused or allowed for one reason, in one place | **DONE 2026-08-29**, in nine commits (0.26.0–0.34.0). The gate is one place and one order, and the loop it feeds is playable: point at a tree, click or tap, and it falls. 0.26.0 `ActorOccupancyHelper` + `ActorTracker` · 0.27.0 `WorldObjectDeathRules` + the three authored fields nothing read (`tier`, `allowedTools`, nullable `toolType`) · 0.28.0 `HeldItemComponent` — the selected slot IS the hand, derived not stored; an empty slot is BARE HANDS for a player · 0.29.0 `WorldObjectPermissionHelper` + `WorldObjectToolHelper` — the ordered gate, both order traps under test, target as a sealed `DamageTarget` (ground is a TILE here, FP3.4) · 0.30.0 props gained health and drops; a death releases its tiles BEFORE the loot rolls, and the spawn system forgets a corpse (a double free that `freePropTiles` asserts on) · 0.31.0 `IActor.usePrimaryActionOn` + `attackDamage` · 0.32.0 the unified cursor in `InputHelper` (mouse + touch) · 0.33.0 `AimSnapshot` — measured once, never re-derived · 0.34.0 `ActorPlayer`: strict single-tile targeting, edge-to-edge reach, `1/base_action_speed` pacing, rule-30 blocker. Manual page `gathering.md` in both languages. **What FP4.3a deliberately did NOT do, each with its subject named:** the swing has no MOTION on screen (`HeldItemComponent`'s tween half, with the impact delay `AimSnapshot` exists to survive); actors cannot be hit (no corpse, loot or respawn — so `usePrimaryActionOn` takes a `Prop`, a missing door rather than a branch that answers no — 0.38.0 renamed it `usePrimaryAction(AimSnapshot)`, and actors still cannot be hit); the GAMEPAD's virtual cursor is unported, which is rule 12 broken in writing (it also blocks FP4.2b's mouse-wheel hotbar); and the action costs — energy, mana, combo points — wait with the rest of `HeldItemComponent`. |
| 9 | FP4.3b — placement | `implementation_plan/FLUTTER_PORT_PLAN_2026-08-25.md` | a blueprint in hand puts the thing into the world, or says why not | **DONE 2026-08-30**, in six commits (0.35.0–0.40.0). With a blueprint in hand a press puts the thing into the world, and a see-through copy shows where it would land — green where it may, red where it may not. 0.35.0 `ItemBuildableData` (a `blueprint_id`, the blueprint resolved through a registry) + type routing in `ItemRegistry`; five documents imported from the spec, and the loot-id sweep learned to cover GROUND · 0.36.0 `WorldPlacementHelper.placePropRefusal` + `enum PlacementRefusal` — a refusal that is NAMED — and `GridManager.canPlacePropAt`; occupancy is the FIRST gate, above every early return, and two tests fix the order by refusing a tile two gates refuse and asserting which one answers · 0.37.0 the ground half (water and cliff are replaced) + `Events.groundTileChanged` → `GroundChunkRenderer` re-bakes the chunk · 0.38.0 REFACTOR: `ItemHand` + `ItemHandTool`, `usePrimaryAction(AimSnapshot)`, `HeldItemComponent.hand` picking the hand, and `enum ActionOutcome {none, spent, landed}` — vocabulary this port invented, not the spec's · 0.39.0 `ItemHandBuildable`, the verb itself in four steps (tiles → gate → spend → place), `isAreaWithinRange` extracted so building and striking share ONE geometry, `ProceduralSpawnSystem.adoptPlacedProp` so a built prop leaves with its chunk · 0.40.0 `BuildGhostRenderer` + `previewAt` — the preview and the press read the SAME resolution (`BuildPreview`), so the ghost cannot lie about the click. **A seventh slice was planned, investigated and REFUSED:** `canBuildOnTarget` in `WorldObjectPermissionHelper`. Its subject is no longer the missing data class — `ItemBuildableData` exists since 0.35.0 — but the missing SECOND CONSUMER: `canDamageTarget`'s only caller here is `ItemHandTool.primaryAction`, and `_rebuildHand` routes every blueprint to the build hand before the tool arm, so the source can never hold one when that gate is asked. The spec's branch is reachable only because `interaction_indicator.gd` asks it too, and that indicator is FP5.1. The helper's own note now says this (0.44.0). **Two debts written down rather than forgotten:** (1) **nothing in the game can obtain a blueprint** — no prop drops an `item_buildable_*`, the three seed blueprints belong to `stage_drop_configs` (FP4.4), grass comes from destroying ground (FP7), and the smelter comes from breaking a smelter nobody placed; the player authors no starting inventory. Placing becomes reachable at **FP4.5**, whose crafting menu authors the smelter at `crafted_at: NONE`. That is why 0.39.0 and 0.40.0 are `### 🧹 Internal` and not `### ✨ New`, and why the manual page `building.md` is DEFERRED to the FP4.5 commit that makes the verb reachable — documenting a deed the player cannot perform would be a lie. (2) **stacking ground on ground is refused WHOLE** (`cannotStack`), not half-ported: the spec's first gate alone would leave a branch that says yes to the terrain and then has nowhere to go, because max height, the world edge and the slab margin are all FP7. **Also true and deliberate:** a placed tile is DATA, not a node (terrain is nodeless, FP3.4); and a built prop or bridge DISAPPEARS when its chunk recycles, the same bargain a harvested prop already makes — surviving an unload is FP6. Ledger `L-006` and `L-007` were opened by this work. |
| 10 | FP4.5 (e)–(h) — the door to the bench, the two surfaces, the loadout, the pages | `implementation_plan/FLUTTER_PORT_PLAN_2026-08-25.md` | a new world with the pickaxe in slot 1: mine, punch a palm, hand-craft the smelter, place it, produce a bar | **re-sliced 2026-09-10.** (e) `InteractableComponent` + `IActor.tryInteract(AimSnapshot)` on the same edge-to-edge reach as the swing — the hover indicator is NOT needed for the verb (the aim carries the tile), it is FP5.1(e); the key is `D-1`. (f) `WorkstationPanelView` + the hand-craft surface (`D-2`), strings with the spec's spellings, and the first `display_name_key` reader (pays #7's sort-axis delta). (g) **landed 0.50.0** — step 26 twin, `IStartingLoadoutData`, `LoadoutRegistry`, `StartingLoadoutRules.apply(player)` at boot, the pickaxe document imported, the pack's loadout authoring one pickaxe. **CORRECTION of the 2026-08-31 decision:** the spec has authored a starting loadout since `tessera 0.334.0` (`data/progression/starting_loadout_default.md`, `IStartingLoadoutData`, `StartingLoadoutRules`, step 26) — no new field on the player document, no fork of the contract; the port is the step-26 twin + the rules, and the pack copy here authors one copper pickaxe. Chain verified against both packs: the palm falls to bare hands (`INNATE`), smelter and workshop are `crafted_at: NONE`, every tool is `WORKSHOP` — one item opens the whole loop. (h) `crafting.md` + `building.md`, `### ✨ New` for craft and place together |
| 11 | FP4.4 — farming, after FP4.5 | same plan | till, water, plant, three day boundaries, harvest wheat — with the FP4.5 loadout only | **sliced 2026-09-10; the crop half started the same day (0.49.0)** because FP4.5(g)'s chain needed it: a palm's logs live only in its BUDDING stage table, which nothing read, so no felled palm gave wood. In: `PropCropData` (no soil), `CropRules`, `CropDropComponent`, `PropCrop` without growth, the scatter rolling a stage in `[PLANTED, peak]` (the spec's order, not "at peak"), `CropRenderer` (every palm drew row 0 before), seven seed documents imported. Still here: (a) `TimeSystem`, soil, (d) the ground verb, growth on `dayChanged`, (f), (g) planting, (h). Of the original list: (a) the minimal `TimeSystem` on the fixed step (day boundary at 06:00, `Events.dayChanged`) · (b) `PropSoilData`/`PropCropData` + registry arms, soil flags on the DATA not the component · (c) `CropRules` pure · (d) the ground verb: `farm_tools: [SHOVEL]` transforms terrain into `farm_prop_id` through `canFarmTarget`, asked FIRST in `takeDamage` (rule 33) · (e) `PropSoil`/`PropCrop` hosts, growth and rot on `dayChanged`, `CropDropComponent`; **the scatter must spawn crops at `peak_stage` the moment the palm routes as a crop** · (f) drains `L-007` (`ChunkResidency`) · (g) planting = the place verb + a `needsSoil` refusal, six seed documents imported · (h) `farming.md` (`D-4`). Reachable only after FP4.5: shovel and watering can are workshop recipes |
| 12 | FP0.10–FP0.18 — harness debt | same plan | each script runs from the suite (`--check`) or from rule 32's ritual | **opened 2026-09-10; FP0.10–FP0.14, FP0.16 and FP0.17 done the same day.** FP0.10 `reset_local_save.py` **done 0.58.0** (L-004 drained; the app's identity read from all four platform files and a disagreement refused; both macOS shapes swept; settings kept unless `--all`; FP6.3 still owes the proof against a real save) · FP0.11 `check_pack_snapshot_matches_spec.py` **done 0.56.0** (L-006 drained; documents pair by `id`; a key only the spec authors is the port's backlog, a key both sides author differently is the fork; 28 forked documents and 67 forked keys recorded with a reason each, `L-014` opened; absent sibling = not applicable, never a pass) · FP0.12 `commit_merge.py` + the hook learning `merge --continue` **done 0.61.0** (L-008 drained; and the hook's own env-prefix bypass — `GIT_EDITOR=true git …` walked past every rule — closed in the same commit) · FP0.13 `dawnforge_project` → `tessera_project` sweep **done 0.53.0** (L-003 drained; `SPEC_REPO_ROOT` + `spec_repo_available()` + `spec_pack_root()` written once, cited by every doc) · FP0.14 the `dynamic` tripwire **done 0.57.0** (L-009 drained; scoped to all of `lib/`, zero occurrences so no whitelist; `timeScale` dropped with its reason in the hook header and `AI_HARNESS.md` §3) · FP0.15 **half done 0.63.0**: `check_manual_mirrors.py` ported and in the suite (same filenames, same heading structure, both failures proved); the `README.md` section map + `TEMPLATE.md` wait on `D-4` and the checker says NOT CHECKED for that half until then (L-010 stays open on the map) · FP0.16 `check_changelog_is_ordered.py` **done 0.51.0**, first half of the suite (0.45.4/0.45.3 sat above 0.47.0 until 0.48.0 fixed it by hand) · FP0.17 `check_translation_keys.py` **done 0.52.0**, second half of the suite · FP0.18 `docs/ARCHITECTURE.md` **done 0.72.0** (twelve sections, each stamped with the version it was verified at; the verification's own yield was `L-022`, rule 13's `WorldObjectHelper.getCore` naming a class `lib/` has never contained) — **the block is drained except FP0.15's section map, which waits on `D-4`** |
| 13 | AD1–AD5 — the automation this track owes | `implementation_plan/AUTOMATION_DEBT_2026-09-10.md` | each owed script runs from the suite (`--check`) or from the commit ritual | **opened 2026-09-10** (0.54.0), measured at 0.53.0 against `tessera 0.440.4`. The spec runs 27 pipeline steps, 40 project commands, 12 harness commands and 10 hooks; this repo runs 7, 4, 2 and 2 — and **three quarters of that gap is not debt**: the voxel dimension (steps 13–22, 27, 28), the ten-lane board and pack AUTHORING (28+21 scripts) do not cross, each with its reason written down. What is owed: `--check` for steps 02/03 and step 99 (`L-012`), the coverage guard that finds them, the decision-ID guard (`L-013`), the asset-reference guard (`L-015`), the version-agreement, clean-compile, debug-marker and noisy-suite guards, `stamp_changelog_section.py` (the `sed` in `CLAUDE.md` has already cost this repo one hand repair), the four content validators, and four harness commands (a test over the guards themselves, a ledger index, a decision inbox, a runway report). Three forks: `AD-D1` step numbering, `AD-D2` whether anything ships, `AD-D3` the commit script |
| 14 | TT1–TT6 — the code no test has ever named | `implementation_plan/TEST_TRACEABILITY_2026-09-10.md` | the never-named count goes down and never up | **opened 2026-09-10** (0.55.0), measured at 0.53.0 over 381 green tests: of 109 files under `lib/src/` that declare a class, **88 are named somewhere in `test/` and 21 are not**. Named is an upper bound on coverage and never-named is a lower bound on dark code — that is the number the items use. In: the four foundations (`JsonReader`, the parse path rule 5 lives in; `Splitmix32`, why a seed reproduces a world; `DropEntry`; `AllocatedMaterial`), the three FP1.5 components the index records as done while only their RULES are tested, the hand (`HeldItemComponent`, 190 lines, the port's own derived-not-stored invention), the envelope FP6 is about to stand on (`serializeEnvelope` appears in no test file), and the four render classes — reachable today with the idiom `test/core/render/dawnforge_game_test.dart` already uses, so nothing enters `pubspec.yaml`. `CropDropComponent` landed 0.49.0 with no test, which is the policy slipping in the present tense. One fork: `TT-D1`, whether the ratchet refuses or reports |
| 15 | PI1–PI10 — the pack content still to import, and the fields inside what already crossed | `implementation_plan/PACK_IMPORT_MAP_2026-09-10.md` | each batch lands with its data class, every id resolving, the pipeline `--check` green and the drift check clean | **opened 2026-09-10** (0.59.0), measured at 0.53.0: **930 authored documents in the spec pack, 64 here.** The almanac is a ladder of five tiers of ~107 documents each and this port stands on the first rung (t1 52 of 109, t2 7 of 106, t3–t5 none) — so tiers 2–5 are volume, not risk, and what gates them is `is_content_unlocked` (FP7.5) and the tier field (FP7.3), never the importer. The sharpest number: **1 of the workshop's 115 documents is here**, because imports have been driven by what one commit needed and never by what a station is. Batches: the three stations already here (210) · the forge and kitchen (80) · the second biome, which is FP7.3's own gate (99) · audio, whose absence is the 37 unresolved `res://` references already emitted (1 + 114 sounds) · quests then achievements (36 + 131, and the generator crosses with them) · cosmetics (166, whose ids the player document already names) · the five caves (5) · `ui_anim_config.md` (1) · tiers 3–5 (319). Never crossing, with reasons: the design prose, the templates, the island documents, the rigs and the Godot import sidecars. **§2b added 0.61.0:** the 59 documents that DO pair are pruned copies — rule 6 forbids authoring a field with no data class — so they are **932 authored keys behind** across eight blocks. That table is the FP7 backlog in field form: `ItemData`'s six motion fields are FP7.7's swing-with-no-motion (26 items author them, nothing reads them), `base_max_stamina` is the action costs, `interaction_prompt`/`interaction_range` are FP4.5(e) measured at 14 and 13 documents, and the 525 keys under `IVisualObjectData` are one field family — juvenile and void frame sets — waiting on one renderer (FP7.2 breeding, FP7.11 cosmetics). One fork: `PI-D1` |
| 16 | FP8.1 — the second-pack probe | `implementation_plan/FLUTTER_PORT_PLAN_2026-08-25.md` | a second pack boots and `git diff --stat lib/src/core/` is empty | **opened 2026-09-10** (0.66.0). Study §9 clause 5 — *the actual point of the study* — had never been probed, and it depends on no phase: it can be run today and gets cheaper the earlier it fails. Measured first: the Dart side names its game **once** (`game_constants.dart:5`), everything else asks `ContentPaths`; the surface that is not one place is `pubspec.yaml`'s 27 hand-written asset folder lines, which is where the probe will most likely bite and is not a `lib/` problem. The probe pack is two documents plus the player and progression pair — deliberately not a game |
| 17 | IP1–IP9 — the presses the port does not have, and the table that says when a press may act | `implementation_plan/INPUT_PARITY_2026-09-10.md` | each owed action leaves the table with a subject, a test and a device that can send it | **opened 2026-09-10** (0.68.0), measured at 0.67.0 against `tessera 0.440.4`. Rules 11 and 12 are stated as law and had never been measured: the spec's `dawnforge` binds **51 actions** — 37 keyboard, 2 mouse, 36 gamepad, **zero touch** — and this port answers **22**, with 20 from the keyboard, 1 from a pointer, **0 from a gamepad** and 1 from a finger. Three of the 51 never cross (`jump` and the two `camera_rotate_*` are the voxel dimension, decided by the arbiter table itself), so **26 are owed**, each filed under the FP step that gives it a subject. Two findings a rule alone could not produce: **three bindings deliberately disagree** with the spec, and together they make `Tab` mean *open the bag* here and *previous hotbar row* there — free to settle now, expensive once a manual page prints it; and **the port's device story is inverted** (they have a pad and no finger, we have a finger and no pad), so rule 12 reads as broken only from one side. `L-020` opened: the context of a press is decided at five scattered subscribers instead of once at the source, which is why a hotbar digit still changes the hand with the bag open, and why `onKeyEvent` claiming every key will make the first text field unreachable. The catalog port DELETES two things — the duplicated context enum (`UIState` already mirrors it; GDScript could not reach it, Dart can) and the label/context table split — so it is smaller here than there. Three forks: `IP-D1` how a gamepad reaches Flutter at all, `IP-D2` what touch answers, `IP-D3` whether bindings can be rebound |
| 18 | ES1–ES10 — the engine itself, family by family | `implementation_plan/ENGINE_SURFACE_2026-09-10.md` | each family's count re-measured by script, never adjusted by hand | **opened 2026-09-10** (0.69.0), measured at 0.68.0 against `tessera 0.440.4` with `git ls-files`. The spec's engine is **560 files** across `shared/` and `world2d/`; this port is **112**, one in five — and the fifth that is here is the load-bearing fifth: `factories/` **4 of 4**, `registries/` 7 of 12, `domain/` 13 of 40, against `ui/` **4 of 87** and `systems/` 17 of 124. `world3d/` is a further 42 files that never cross. The actionable family is **ES1**, the 40 pure `*Rules` classes FP7.7 itself calls the cheapest ports in the tree: 12 have a twin, and the raw subtraction lies twice — `IslandSpawnRules` is the island mode, and `PlacementRules`/`tool_refusal_rules` are already here as `WorldPlacementHelper` and `WorldObjectToolHelper`/`WorldObjectPermissionHelper` — so **25 are owed**, each filed under the step that gives it a caller (20 of them FP7.7). `ES3` is deliberately NOT planned from this table: `PACK_IMPORT_MAP` §2b measures the same gap in the unit that decides it, a field rather than a file. Two rows carry their own story: the **16 network files** are study decision D4 in file form, which is the second half of `L-018`/`D-9`'s evidence — the multiplayer shape is being kept by intention alone against a sibling that has 16 files of it; and **`render/` has no twin in 560 files**, 8 files and 1,081 lines that exist only because Flame does not draw for you, sitting inside FP8.1's zero-diff gate (`L-023`, `ES-D2`). Two forks: `ES-D1` the spec's one code-enforced exception to rule 5 (recommend refusing it — rule 32 plus `reset_local_save.py` already answer it), `ES-D2` whether `render/` is a family or an implementation detail |
