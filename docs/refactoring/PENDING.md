# Pending — everything documented and not yet done

> The derived index over the plans in `implementation_plan/`. Each item names its plan,
> its gate, and its state. Read this first when onboarding (the `/onboard` flow).

| # | Item | Plan | Gate | State |
|:--|:---|:---|:---|:---|
| 1 | FP0.9 — port the lifecycle skills | `implementation_plan/FLUTTER_PORT_PLAN_2026-08-25.md` | skills fire on their triggers | pending (rest of FP0 gate met 2026-08-25) |
| 2 | FP1.5/FP1.9 — component slice + domain `*Rules` ports | same plan | first components (health/movement) + rules with tests | first slice done 2026-08-26 (Direction/Movement/Health + 4 Rules); more vitals/AI components arrive with their systems |
| 3 | FP2.3 — sprites steps (02/03) | same plan | atlas cropped into sprite PNGs | done 2026-08-26 (22 crops + 14 placeholders; Pillow; `res://data/…` mapped to `assets/generated/<game>/…`). FP2.4/FP2.5 done 2026-08-26. Note: the Godot repo restructured (`tessera.py`, pipeline under `tessera/scripts/`) — cross-repo references in our docs may need a sweep |
| 4 | FP3 — World & rendering (Flame) | same plan | walkable chunked world at 60fps with budget overlay | **gate met 2026-08-26** — hand-run on the macOS app held **120fps** (the display's own ceiling) walking one direction continuously, which is the load pattern that stresses streaming hardest: load at the leading edge and unload at the trailing one, every chunk border, plus each new chunk's spawn burst since 0.19.0. A sustained 8.3ms frame against a 1200µs stream budget is ~2× the headroom the 60fps gate asked for. FP3.1–3.3 done 2026-08-26; FP3.4 in five commits (0.8.0–0.12.0); FP3.5 grid collision (0.13.0); FP3.6 overlay (0.14.0) |
| 5 | FP4 — First gameplay loop | same plan | harvest → craft → place playable | FP4.1 done 2026-08-26 in four commits (0.15.0 loot tables + biome population parse, 0.16.0 DropRules, 0.18.0 physical pickups, 0.19.0 procedural scatter + prop occupancy); FP4.2a inventory domain done (0.17.0). **FP4.2b done 2026-08-27** in seven commits — see #7. **Remaining after it: FP4.3** (verbs/permissions, then place), **FP4.4** (farm transform), **FP4.5** (crafting — the gate). Ground destruction deferred to FP7 by decision |
| 6 | FP5–FP7 | same plan | per-phase gates | pending. FP5.1's `UIStateMachine` + back-press arbiter arrived early with FP4.2b (0.22.0) — what remains there is the HUD itself and the notification queue |
| 7 | FP4.2b — hotbar + inventory surface | same plan | the bag is openable, draggable and readable in both languages | **done 2026-08-27.** 0.20.0 interface strings enter the pipeline (`data/ui/`, step 05's second source) · 0.21.0 slot verbs · 0.22.0 `UIStateMachine` + rule-25 arbiter (pulled forward from FP5.1) · 0.23.0 the player becomes an authored actor with a 30-slot bag (was a boar with none; the pipeline learned multiple object roots) · 0.24.0 the hotbar · 0.24.2 the authored `pickup_delay`, without which nothing can be put down · 0.25.0 the panel (drag swap/merge, Shift-drag half, drop-to-world, live `BackdropFilter`). **Two things deliberately NOT in it:** the Sort button, which needs `InventorySortRules`' item subtypes and `tier` (FP4.3b, FP4.5); and mouse-wheel hotbar cycling, which needs pointer state in `InputHelper` (rule 11 forbids reading it anywhere else). The spec's carry-state split is not planned — Shift-drag replaced it by decision |
| 8 | FP4.3a — verbs & permissions | `implementation_plan/FLUTTER_PORT_PLAN_2026-08-25.md` | a swing is refused or allowed for one reason, in one place | **in progress 2026-08-27, five of six slices done.** 0.26.0 `ActorOccupancyHelper` + `ActorTracker` (the `character`-group twin) · 0.27.0 the tool gate: `WorldObjectDeathRules.{hasAllowedTool,meetsTierRequirement}` plus the three fields the pack already carried and the data classes did not read (`tier` on `IVisualObjectData`, `allowedTools` on `IWorldObjectData`, nullable `toolType` on `ItemData`) · 0.28.0 `HeldItemComponent` — the selected slot IS the hand, derived on read rather than stored, off `selectionChanged`'s two meanings; **an empty slot is BARE HANDS for a player** (`t{tier}_item_tool_melee_hand`, the `INNATE` tool), which is what (5) will swing when nothing is equipped, and the innate tier is the one line FP7's `TierSystem` changes · 0.29.0 `WorldObjectPermissionHelper` + `WorldObjectToolHelper` — the ordered gate, with both of the spec's order traps under test (the farm bypass ABOVE the occupancy rule, and ground exempt from that rule because `canGroundBeDestroyed` already asks it with an extra clause). A target is a sealed `DamageTarget` — `ObjectTarget` for a host, `GroundTarget` for a TILE, because terrain is nodeless here (FP3.4) — kept as ONE argument to ONE gate so the two orders cannot drift. · 0.30.0 + 0.31.0 the damage verb, in two halves. **0.30.0 the death:** props gained health and drops, `Prop.takeDamage` re-asks the gate AT THE BLOW (every path into the world meets it, so a gate living only in the helper is one caller away from a hole), and a death releases its tiles BEFORE the loot rolls — a colliding corpse still holding its tile pushes its own drops off it. `ProceduralSpawnSystem` now forgets a felled prop, or the chunk's unload frees its tiles a second time and `freePropTiles` asserts. `PropFactory.create` gained a `random` seam so FP4.1c's seeded-drop test keeps its point. **0.31.0 the swing:** `IActor.usePrimaryActionOn` + `attackDamage` on `ItemData` (authored in the pack since the import, unread until now). Its parameter is a `Prop`, not the gate's `DamageTarget`: a prop is the only thing here with a death to reach, and widening it before actors have a corpse would let the cursor promise a blow the world cannot deliver. **Remaining:** (6) the `ActorPlayer` host — action range, aim (`aim_snapshot.gd`) and the input that presses the button; its DATA landed in 0.23.0. **It needs something nothing has needed yet: POINTER STATE in `InputHelper`** (rule 11 forbids reading a pointer anywhere else, rule 12 wants the same behavior from gamepad and touch), and the swing's PACING (`_reset_action_cooldown` off the authored `base_action_speed`), which belongs with the button that fires it. **The two things the spec's own comments insist on — ported and under test in 0.29.0, and still the two that a change here would get wrong:** the farm/build/watering bypasses sit ABOVE the occupancy rule in `can_damage_target` on purpose — tilling, watering and planting neither add nor remove the ground anyone stands on, so they stay legal under your own feet; and ground is EXEMPTED from the occupancy clause there because `can_be_destroyed` already asks the same question with its own extra clause — asking twice is the third copy that caused the bugs `ActorOccupancyHelper` exists to prevent |
