/// [GameplayTileConstants] responsible for centralizing game mechanics configuration
/// Following Flutter naming conventions for game systems
class GameplayTileConstants {
  static const double kCurrentTileSize = 32.0;

  // Tile system constants
  static const double kDefaultTileSize = 16.0;
}
